# To run:

```bash
python -m venv venv
source ./venv/bin/activate
pip install -r requirements.txt
python dynamic_trader_v2.py
python status.py
```
dynamic_trader_v2 is the code that open/closes trades
status.py will compare the result of dynamic_traver_v2 to 

